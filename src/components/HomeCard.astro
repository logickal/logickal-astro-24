---
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';

interface Props {
    title: string;
    image: string;
    link: string;
}

const { title, image, link } = Astro.props;
const images = import.meta.glob("../assets/imgBg/*");
let classes = null;


---




<a class="m2 p-3 w-4/6 h-1/5" href={link}>
    <div class="w-1/6 flex justify-end bg-white p-2 transition-all duration-75 hover:w-2/6">
        <h3 class="text-lg font-mono">{title}</h3>
    </div>