---
import Header1 from './atoms/headers/Header1.astro'

const { tagText } = Astro.props;
let tagLine = '';

if (tagText === 'all') {
    tagLine = 'All logickal releases';
} else {
    tagLine = `Logickal releases tagged ${tagText}`;
}
---

<div class="header m-5 p-5 bg-gray-800 fixed top-0 left-0 right-0 z-10">
    <div>
        <Header1>Discography - {tagText}</Header1>

        <p class="font-mono">{tagLine}</p> 
        <div class="pt-2 font-mono text-sm text-right">
          <p><a href="/discog" class="hover:text-blue-400">genre selection</a></p>
          {tagText === 'all' ? '' : <p><a href="/discog/all" class="hover:text-blue-400">all releases</a></p>}
        </div>
    </div>
</div>